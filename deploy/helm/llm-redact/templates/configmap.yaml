apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "llm-redact.fullname" . }}-config
  labels:
    {{- include "llm-redact.labels" . | nindent 4 }}
data:
  config.toml: |
    {{- include "llm-redact.configToml" . | nindent 4 }}
